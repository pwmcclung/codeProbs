def tunnel_digging(r):
    times = {
             '[]':60,'{}':50, '()':40,'||':30, '::':20, '  ':0, 
             '[':30, ']':30,'{':25, '}':25, '(':20, ')':20, '|':15, '|':15, ':':10,
             '[}':55, '{]':55, '[)':50, '(]':50, '[|':45, '|]':45, '[:':40, ':]':40, ' ]':30, '[ ':30, 
              '{)':45,  '(}':45, '{|': 40, '|}':40, '{:':35, ':}':35, '{ ':25, ' }':25,
              '(]':50, '[)':50,'(|':35, '|)':35, '(:':30, ':)':30, '( ':20, ' )':20, 
              '|:': 25, ':|':25, '| ':15, ' |':15, 
               ': ':10, ' :':10
             }
    work_time = 0
    count = 0
    length = (len(r)//3)*30
    for x in range(len(r)):
        first = r[x]
        work_time += times[first]    
    return work_time+length